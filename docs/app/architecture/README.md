# Architecture

Feature-folder layout, the `app/` vs `mystic_auth/` split in practice, and why it's structured the way it is.

- **[overview.md](overview.md)**: the full backend/frontend directory tree
- **[conventions.md](conventions.md)**: why routes, API clients, and access code are split the way they are
- **[sdk-and-extension-points.md](sdk-and-extension-points.md)**: the `sdk`/`app_sdk` split, and how the sidebar/dashboard extend mystic_auth's shared chrome without editing it
